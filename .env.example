PORT=3000
ADMIN_USERNAME=admin
# Generate ADMIN_PASSWORD_HASH with:
# node -e "const crypto=require('crypto');const p='change-me';const salt=crypto.randomBytes(16).toString('hex');const hash=crypto.scryptSync(p,Buffer.from(salt,'hex'),64).toString('hex');console.log(`${salt}:${hash}`)"
ADMIN_PASSWORD_HASH=replace-with-salt-and-hash
JWT_SECRET=replace-with-a-long-random-secret
